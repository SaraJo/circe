# Windows beta implementation

1. Add prompt-based persistent tab names and install the Mac update.
2. Resolve native Windows Hermes paths; propagate HERMES_HOME to child processes.
3. Normalize watcher paths and use platform-appropriate window controls and menus.
4. Add installer command, NSIS configuration, Windows CI, and targeted tests.
5. Cross-build the installer and document the remaining Windows runtime checks.

See the matching design and docs/WINDOWS.md for scope and acceptance criteria.
